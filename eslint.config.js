import { define } from '@remcohaszing/eslint'

export default define([
  {
    rules: {
      'no-console': 'off',
      'import-x/no-extraneous-dependencies': 'off',
      'n/hashbang': 'off'
    }
  }
])
